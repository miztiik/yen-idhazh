# GitHub signs the squash commit it creates on every merge, so its own account
# lands in the committer field and no rewrite can reach it. Folding it keeps
# `git log`, `git shortlog` and `git blame` reporting the one name that did the
# work. Git reads this file only from the repository root.
miztiik <miztiik@users.noreply.github.com> <noreply@github.com>
