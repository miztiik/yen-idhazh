---
name: bootstrap
description: Find which page owns the thing you are about to change. Use when a task touches a subsystem you have not read, or when you cannot place where a rule or a persisted shape lives. Not a precondition for every answer.
---

# bootstrap

A pointer, not a procedure. The routing table lives in [`docs/agents/bootstrap.md`](../../../docs/agents/bootstrap.md): which page owns each kind of change, and what every answer owes.

## What you must do

1. Open [`docs/agents/bootstrap.md`](../../../docs/agents/bootstrap.md) and find the row that matches the change.
2. Read the page that row names. Not the whole tree, and not the contract end to end.
3. When editing agent/customization Markdown, use ASCII only: "-", "->", ">=", "section".

## See also

- [`docs/concepts/pipeline-loop.md`](../../../docs/concepts/pipeline-loop.md) - the stages and what each one owns.
- [`CLAUDE.md`](../../../CLAUDE.md) - the engineering contract; section 1 is the guardrails every persona must honour.
