{
  "method": "sum of gzip -9 over each module in the route's rel=\"modulepreload\" list, per file, never concatenated",
  "toolchain": "node 22",
  "tolerance_bytes": 64,
  "tolerance_basis": "4 builds of identical source, route /, 2026-08-25, this repo: 49193 / 49198 / 49198 / 49205. Range 12 B, and the cause is named - SvelteKit stamps a version string into the entry chunk. 64 B is 5.3x that range; four samples underestimate a true range, so the multiple is the margin. Derived, not measured. If a build ever fails inside 64 B, re-derive it from more builds and more routes rather than nudging it.",
  "routes": {
    "/": {
      "bytes": 49717,
      "measured": "2026-08-29",
      "why": "Up for the fluid frame. The shell stopped being a fixed 672px box and became a frame that grows to config/appearance.json frame.reading_max_px, and the reading measure moved off the shell onto the title, the summary and the reader note - measured 2026-08-28, one max-w-2xl on the root layout capped every page at 624px and left 912px of a 1536px screen empty. The bytes are the item's own scoped rule: a one-column grid that becomes a body and a sticky meta rail above 1024px, so source, date and confidence stop interrupting the read. Read as a difference: origin/main's source built on this tree, this machine and this node read within 13 B of every record, so this is the change and not the toolchain. The frame VALUES cost nothing here - they are generated into src/styles/frame.generated.css at build time, because injecting them from the layout head instead was measured at 397 to 700 B of JavaScript on every route, including two that render nothing."
    },
    "/<date>/": {
      "bytes": 49591,
      "measured": "2026-08-29",
      "why": "Up for the fluid frame. The shell stopped being a fixed 672px box and became a frame that grows to config/appearance.json frame.reading_max_px, and the reading measure moved off the shell onto the title, the summary and the reader note - measured 2026-08-28, one max-w-2xl on the root layout capped every page at 624px and left 912px of a 1536px screen empty. The bytes are the item's own scoped rule: a one-column grid that becomes a body and a sticky meta rail above 1024px, so source, date and confidence stop interrupting the read. Read as a difference: origin/main's source built on this tree, this machine and this node read within 13 B of every record, so this is the change and not the toolchain. The frame VALUES cost nothing here - they are generated into src/styles/frame.generated.css at build time, because injecting them from the layout head instead was measured at 397 to 700 B of JavaScript on every route, including two that render nothing."
    },
    "/<date>/<topic>/": {
      "bytes": 49684,
      "measured": "2026-08-29",
      "why": "Up for the fluid frame. The shell stopped being a fixed 672px box and became a frame that grows to config/appearance.json frame.reading_max_px, and the reading measure moved off the shell onto the title, the summary and the reader note - measured 2026-08-28, one max-w-2xl on the root layout capped every page at 624px and left 912px of a 1536px screen empty. The bytes are the item's own scoped rule: a one-column grid that becomes a body and a sticky meta rail above 1024px, so source, date and confidence stop interrupting the read. Read as a difference: origin/main's source built on this tree, this machine and this node read within 13 B of every record, so this is the change and not the toolchain. The frame VALUES cost nothing here - they are generated into src/styles/frame.generated.css at build time, because injecting them from the layout head instead was measured at 397 to 700 B of JavaScript on every route, including two that render nothing."
    },
    "/404": {
      "bytes": 40753,
      "measured": "2026-08-27",
      "why": "Runtime and app shell and nothing route-specific. This is the floor every other route sits on. It fell 105 B in the same chunk re-cut that raised /: one module left this route's preload list. Control on this tree: 40,858 B."
    },
    "/archive/": {
      "bytes": 51115,
      "measured": "2026-08-29",
      "why": "Up for the fluid frame and the archive's columns. The browse list splits on the width it HAS - repeat(auto-fit, minmax(22rem, 1fr)) - so it is one column on a phone, two above about 1024px and three above about 1400px. The archive is the one list on the site that goes multi-column: it is a scan surface where a reader is finding the story they remember, so \"find it fast\" outranks the ranked order the digest has to protect. Asking the viewport instead of the container is the bug that drew three console charts at 164px each, and it is not repeated here."
    },
    "/console/": {
      "bytes": 69622,
      "measured": "2026-08-29",
      "why": "Runtime and shell plus the console route: hand-written SVG charts and the d3-scale and d3-array arithmetic behind them, the stage-timing decade axis with its minor stubs and its gap-instead-of-plunge run splitting, the Charts table that says what the router cost and what it published on each day, and the What the model did table. Up 212 B, which is 0.3 percent of the route, because the compression scatter now decides where each article sits in the browser instead of on the server: one pass over the telemetry rows the viewport already holds, keeping one row an article a day and folding it into a point, a row with no length or a row with no summary. It is bought with 27,092 B off the document. The scatter used to be built at build time from every row state/scores.csv has ever held - 2,791 of them on 2026-08-29, growing about 349 a day with nothing trimming the ledger - and each point was inlined into the prerendered page. It now reads the published telemetry projection, which is seeded to console.default_window_days and grown by month fetch, so the plot costs the page nothing it was not already carrying. Read as a difference: origin/main's source built on this tree, this machine and this node read 69,410 B against a record of 69,411, so the 212 B is this change and the -1 is the toolchain. Two builds of the branch read 69,622 and 69,617, so the spread is 5 B against a tolerance of 64. The prerendered console document fell 175,892 to 148,800 B gzip -9 over the same committed ledger, a 15.4 percent drop, and the room left under the ceiling went from 125,688 to 152,780 B."
    },
    "/evals/": {
      "bytes": 41710,
      "measured": "2026-08-27",
      "why": "Runtime and shell plus the stub that moves an old bookmark to /console/. Down 98 B in the same chunk re-cut. Control on this tree: 41,808 B."
    }
  }
}
